{
  "name": "mypetfit-rules-test",
  "version": "1.0.0",
  "private": true,
  "description": "Security-rules test suite for firestore.rules and storage.rules. Runs entirely against the local Firebase emulator on the demo-mypetfit project id, which the emulator guarantees never contacts production.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore,storage --project demo-mypetfit \"mocha rules.test.js --timeout 20000\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "mocha": "^10.7.3"
  }
}
